name: Publish site with Google reviews

on:
  schedule:
    - cron: "17 10 * * *"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    uses: leonardomarquizelli25-tech/recanto-florenca-site/.github/workflows/update-google-reviews.yml@main
    with:
      place_id: ChIJzZCkmnnav5QRE-cTdB3U__8
      source_ref: main
      custom_domain: recantoflorenca.com.br
    secrets:
      GOOGLE_PLACES_API_KEY: ${{ secrets.GOOGLE_PLACES_API_KEY }}
